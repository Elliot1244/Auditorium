using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class InventoryScript : MonoBehaviour
{

    //private TextMeshProUGUI _graalText;
    // Start is called before the first frame update
    void Start()
    {
        //_graalText.GetComponent<TextMeshProUGUI>();
    }

    // Update is called once per frame
    //public void UpdateGraalText(GraalScript graal)
    //{
    //    _graalText.text = graal.ToString();
    //}
}
